/** @format */

import { Button, ButtonProps } from "@mui/material";
import styles from "../assets/css/components/ProductCard.module.css";

export const ProductCard = (props) => {
  return <div className={`${styles.container}`}></div>;
};
